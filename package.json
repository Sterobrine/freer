{
  "name": "freer",
  "private": true,
  "version": "0.4.0",
  "scripts": {
    "dev": "pnpm --dir gui dev",
    "build": "pnpm --dir gui build",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "api:types": "pnpm --dir gui api:types"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0"
  }
}
